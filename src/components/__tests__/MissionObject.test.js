import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import MissionObject from '../MissionObject.vue'

describe('MissionObject', () => {
  it('renders properly', () => {
    // TODO
  })
})
